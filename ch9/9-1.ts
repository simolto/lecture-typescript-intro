// 9-1. 클래스 - 소개

// 설명: 자바스크립트에서 클래스는 ES2015(ES6) 문법입니다.
//  이는 기본적으로 인스턴스를 만들어 주는 역할을 하는데요,
//  new 키워드와 함께 인스턴스를 생성하면 클래스로 정의한 내부 로직이 실행되는데 constructor라는 초기화 메서드를 사용하게 됩니다.
//  이때 인자로 데이터를 넘기면 그것을 토대로 객체 기본 속성이나 API들을 설정할 수 있게 됩니다.
